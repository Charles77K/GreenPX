import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // output: "export",
  // images: {
  //   unoptimized: true,
  // },
  // images: {
  //   loader: "custom",
  //   loaderFile: "./lib/my-loader.ts",
  // },
  /* config options here */
};

export default nextConfig;
